<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pink_start</item>
        <item name="colorPrimaryDark">@color/pink_start</item>
        <item name="colorAccent">@color/pink_start</item>
    </style>

</resources>
